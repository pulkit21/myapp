---
applications:
  .:
    name: blogpost
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    infra: aws
    url: ${name}.${target-base}
    mem: 256M
    instances: 1
    services:
      mysql-5d460:
        type: mysql
